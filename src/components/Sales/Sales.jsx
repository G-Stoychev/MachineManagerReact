export default function Sales({ user, toggleSales }) {
    return (
        <>
            <div>
                <h1>Helloo from Sales</h1>
                <p>{user.name}</p>
            </div>
        </>
    );
}
